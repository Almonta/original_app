<h3><strong><%= t('views.title.customer_edit') %></strong></h3>

<%= render 'form', customer: @customer %>

<%= link_to t('views.button.show'), @customer %> |
<%= link_to t('views.button.back_home'), homes_path %>
