

<p>
  <strong>Product name:</strong>
  <%= @product.name %>
</p>

<% unless @product.user_id == current_user.id %>
  <% if @user_product.present? %>
    <%= link_to 'お気に入り解除する', user_product_path(id: @user_product.id), method: :delete, class: 'btn btn-danger' %>
  <% else %>
    <%= link_to 'お気に入りする', user_products_path(product_id: @producr.id), method: :post, class: 'btn btn-primary' %>
  <% end %>
<% end %>

<%= link_to t('views.button.edit'), edit_product_path(@product) %> |
<%= link_to t('views.button.back_home'), homes_path %>
